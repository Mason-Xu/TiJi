<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.masonxu.tiji">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launch_rectangle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launch_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".setting"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".help"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".Calculate1"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Calculate2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Calculate3"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Calculate4"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Calculate5"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".login"
            android:theme="@style/AppTheme" />
    </application>

</manifest>